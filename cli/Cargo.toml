[package]
name = "battery-cli"
version = "0.1.0"
edition = "2021"
description = "Install Logic Batteries into any SWI/Scryer Prolog project — checksummed, removable, no DataGrout required."
license = "MIT"

[[bin]]
name = "battery"
path = "src/main.rs"

[dependencies]
serde_json = "1"
sha2 = "0.10"
